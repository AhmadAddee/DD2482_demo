[pytest]
addopts = -ra
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: integration tests spanning multiple modules
    io: tests that perform filesystem I/O
